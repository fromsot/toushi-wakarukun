# 作業レポート

## 2026-09-12 思い出機能のComing Soon化

### 背景

iPhoneで撮影した高解像度画像は、HEIC形式の互換性やブラウザ上での圧縮時のメモリ消費により、安定して保存できない場合がある。公開時のデータ保存失敗やブラウザ強制終了を避けるため、改善が完了するまで機能を非公開とする。

### 変更内容

- ホームの「思い出」メニューから起動処理を削除した。
- メニューに `Coming Soon` と表示し、操作できない見た目に変更した。
- `aria-disabled="true"` を付与し、利用不可状態を支援技術に伝えるようにした。
- Coming Soon状態が意図せず解除されないよう、構造テストを追加した。
- PWAの旧CSSが残らないよう、Service Workerのキャッシュ世代を `v9` へ更新した。

### 再公開前の条件

- iPhone実機でHEIC/JPEG写真を検証する。
- 画像を1枚ずつ圧縮・アップロードし、処理後にメモリを解放する。
- 長辺1200px程度、1枚500KB前後を目安にする。
- 通信失敗時の再試行と途中失敗時のロールバックを実機で確認する。

## 2026-09-12 作業終了報告

### 完了内容

- 思い出機能をホームから起動できない `COMING SOON` 状態にした。
- Coming Soon表示と起動処理の非活性を保護する自動テストを追加した。
- Service Workerのキャッシュ世代が `v9` であることを確認した。
- システム設計資料Excelの10シートを確認した。公開サーバ、Supabase Auth/PostgREST/RPC/Storage、GitHub Actions、Cloudflare Pages、Vercel、Migrationの接続・運用経路は記録済み。
- Excel内にTODO・TBD・未入力等の未完了表示や数式エラーがないことを確認した。
- lint、テスト32件、セキュリティヘッダー整合性、本番ビルドはすべて成功した。
- 変更は `c620afd` として `origin/main` へpush済み。

### 現在の状態

- フロントエンド、Supabase連携、PWA構成、CIの基礎は揃っている。
- 思い出機能の実装とDB・Storage構成は残し、公開入り口のみ停止している。
- 作業ツリーはレポート追記前の時点で `origin/main` と同期済み。

### 次回の候補

1. 本番URLで新規登録から収支記録までをiPhone・Android実機で確認する。
2. 5～10人程度の小規模テストユーザーを募集する。
3. 募集文、簡易操作説明、不具合・感想受付フォームを用意する。
4. 利用状況を見て、思い出画像の軽量化・HEIC対応を再開する。
